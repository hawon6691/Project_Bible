package com.pbshop.kotlin.ktor.gradle.exposeddao.postgresql.matching

import com.pbshop.kotlin.ktor.gradle.exposeddao.postgresql.common.StubDomainService

class MatchingService(repository: MatchingRepository) : StubDomainService(repository)
