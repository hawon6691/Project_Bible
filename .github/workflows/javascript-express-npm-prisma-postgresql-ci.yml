name: PBShop javascript-express-npm-prisma-postgresql CI

on:
  pull_request:
    branches: [main, develop]
    paths:
      - 'BackEnd/JavaScript/javascript-express-npm-prisma-postgresql/**'
      - '.github/workflows/javascript-express-npm-prisma-postgresql-ci.yml'
  push:
    branches: [main, develop]
    paths:
      - 'BackEnd/JavaScript/javascript-express-npm-prisma-postgresql/**'
      - '.github/workflows/javascript-express-npm-prisma-postgresql-ci.yml'
  workflow_dispatch:
    inputs:
      target_ref:
        description: 'Branch or tag to run manual jobs on (ex: main, develop, release/v1)'
        required: true
        default: 'main'
        type: string
      run_release_gate:
        description: 'Run release gate summary job'
        required: true
        default: true
        type: boolean
      run_migration_validation:
        description: 'Run shared-schema validation against Prisma expectations'
        required: true
        default: false
        type: boolean
      run_migration_roundtrip:
        description: 'Run temporary-table migration roundtrip check'
        required: true
        default: false
        type: boolean
      run_security_regression:
        description: 'Run security regression E2E suite'
        required: true
        default: false
        type: boolean
      run_admin_boundary:
        description: 'Run admin authorization boundary E2E suite'
        required: true
        default: false
        type: boolean

concurrency:
  group: javascript-express-npm-prisma-postgresql-ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  WORKDIR: BackEnd/JavaScript/javascript-express-npm-prisma-postgresql
  DATABASE_URL: postgresql://project_bible:project_bible@127.0.0.1:5432/pbdb?schema=public

jobs:
  validate-dispatch-inputs:
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - name: Validate Manual Inputs
        run: |
          if [[ "${{ inputs.run_release_gate }}" != "true" && "${{ inputs.run_migration_validation }}" != "true" && "${{ inputs.run_migration_roundtrip }}" != "true" && "${{ inputs.run_security_regression }}" != "true" && "${{ inputs.run_admin_boundary }}" != "true" ]]; then
            echo "At least one manual job must be enabled." >&2
            exit 1
          fi
      - name: Write Validation Summary
        if: always()
        run: |
          echo "## Manual Dispatch Validation" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- Target ref: \`${{ inputs.target_ref }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- run_release_gate: \`${{ inputs.run_release_gate }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- run_migration_validation: \`${{ inputs.run_migration_validation }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- run_migration_roundtrip: \`${{ inputs.run_migration_roundtrip }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- run_security_regression: \`${{ inputs.run_security_regression }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- run_admin_boundary: \`${{ inputs.run_admin_boundary }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- Result: \`${{ job.status }}\`" >> $GITHUB_STEP_SUMMARY

  quality:
    if: github.event_name != 'workflow_dispatch'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ${{ env.WORKDIR }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: BackEnd/JavaScript/javascript-express-npm-prisma-postgresql/package-lock.json

      - name: Install Dependencies
        run: npm ci

      - name: Generate Prisma Client
        run: npm run prisma:generate

      - name: Run Quality Check
        run: |
          mkdir -p test-results
          npm run test:quality | tee test-results/quality.log

      - name: Upload Quality Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: javascript-express-npm-prisma-postgresql-quality
          path: |
            BackEnd/JavaScript/javascript-express-npm-prisma-postgresql/test-results/quality.log
            BackEnd/JavaScript/javascript-express-npm-prisma-postgresql/test-results/quality-check.json
          if-no-files-found: warn

  critical-e2e:
    if: github.event_name != 'workflow_dispatch'
    runs-on: ubuntu-latest
    needs: quality
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: pbdb
          POSTGRES_USER: project_bible
          POSTGRES_PASSWORD: project_bible
        options: >-
          --health-cmd="pg_isready -U project_bible -d pbdb"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=20
        ports:
          - 5432:5432
    defaults:
      run:
        working-directory: ${{ env.WORKDIR }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: BackEnd/JavaScript/javascript-express-npm-prisma-postgresql/package-lock.json

      - name: Install Dependencies
        run: npm ci

      - name: Ensure PostgreSQL Client
        run: |
          if ! command -v psql >/dev/null 2>&1; then
            sudo apt-get update
            sudo apt-get install -y postgresql-client
          fi

      - name: Prepare Environment File
        run: |
          cat > .env <<EOF
          NODE_ENV=test
          PORT=8000
          APP_NAME=javascript-express-prisma
          API_PREFIX=/api/v1
          DATABASE_URL=${DATABASE_URL}
          DB_NAME=pbdb
          DB_USER=project_bible
          DB_PASSWORD=project_bible
          JWT_SECRET=ci-secret-key
          JWT_EXPIRES_IN=1h
          EOF

      - name: Bootstrap Shared PostgreSQL Schema
        env:
          PGPASSWORD: project_bible
        run: |
          psql -h 127.0.0.1 -U project_bible -d pbdb -f ../../../Database/postgresql/postgres_table.sql
          psql -h 127.0.0.1 -U project_bible -d pbdb -f ../../../Database/postgresql/sample_data.sql

      - name: Generate Prisma Client
        run: npm run prisma:generate

      - name: Run Critical E2E
        run: |
          mkdir -p test-results
          npm run test:e2e:critical | tee test-results/critical-e2e.log

      - name: Upload Critical E2E Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: javascript-express-npm-prisma-postgresql-critical-e2e
          path: BackEnd/JavaScript/javascript-express-npm-prisma-postgresql/test-results/critical-e2e.log
          if-no-files-found: warn

  contract-doc:
    if: github.event_name != 'workflow_dispatch'
    runs-on: ubuntu-latest
    needs: quality
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: pbdb
          POSTGRES_USER: project_bible
          POSTGRES_PASSWORD: project_bible
        options: >-
          --health-cmd="pg_isready -U project_bible -d pbdb"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=20
        ports:
          - 5432:5432
    defaults:
      run:
        working-directory: ${{ env.WORKDIR }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: BackEnd/JavaScript/javascript-express-npm-prisma-postgresql/package-lock.json

      - name: Install Dependencies
        run: npm ci

      - name: Ensure PostgreSQL Client
        run: |
          if ! command -v psql >/dev/null 2>&1; then
            sudo apt-get update
            sudo apt-get install -y postgresql-client
          fi

      - name: Prepare Environment File
        run: |
          cat > .env <<EOF
          NODE_ENV=test
          PORT=8000
          APP_NAME=javascript-express-prisma
          API_PREFIX=/api/v1
          DATABASE_URL=${DATABASE_URL}
          DB_NAME=pbdb
          DB_USER=project_bible
          DB_PASSWORD=project_bible
          JWT_SECRET=ci-secret-key
          JWT_EXPIRES_IN=1h
          EOF

      - name: Bootstrap Shared PostgreSQL Schema
        env:
          PGPASSWORD: project_bible
        run: |
          psql -h 127.0.0.1 -U project_bible -d pbdb -f ../../../Database/postgresql/postgres_table.sql
          psql -h 127.0.0.1 -U project_bible -d pbdb -f ../../../Database/postgresql/sample_data.sql

      - name: Generate Prisma Client
        run: npm run prisma:generate

      - name: Run Contract and Doc Export
        run: npm run test:contract:doc

      - name: Upload Contract/Doc Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: javascript-express-npm-prisma-postgresql-contract-doc
          path: |
            BackEnd/JavaScript/javascript-express-npm-prisma-postgresql/test-results/openapi.json
            BackEnd/JavaScript/javascript-express-npm-prisma-postgresql/test-results/routes.snapshot.json
            BackEnd/JavaScript/javascript-express-npm-prisma-postgresql/test-results/contract-doc-summary.json
          if-no-files-found: warn

  perf-smoke:
    if: github.event_name != 'workflow_dispatch'
    runs-on: ubuntu-latest
    needs: quality
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: pbdb
          POSTGRES_USER: project_bible
          POSTGRES_PASSWORD: project_bible
        options: >-
          --health-cmd="pg_isready -U project_bible -d pbdb"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=20
        ports:
          - 5432:5432
    defaults:
      run:
        working-directory: ${{ env.WORKDIR }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: BackEnd/JavaScript/javascript-express-npm-prisma-postgresql/package-lock.json

      - name: Install Dependencies
        run: npm ci

      - name: Ensure PostgreSQL Client
        run: |
          if ! command -v psql >/dev/null 2>&1; then
            sudo apt-get update
            sudo apt-get install -y postgresql-client
          fi

      - name: Prepare Environment File
        run: |
          cat > .env <<EOF
          NODE_ENV=test
          PORT=8000
          APP_NAME=javascript-express-prisma
          API_PREFIX=/api/v1
          DATABASE_URL=${DATABASE_URL}
          DB_NAME=pbdb
          DB_USER=project_bible
          DB_PASSWORD=project_bible
          JWT_SECRET=ci-secret-key
          JWT_EXPIRES_IN=1h
          EOF

      - name: Bootstrap Shared PostgreSQL Schema
        env:
          PGPASSWORD: project_bible
        run: |
          psql -h 127.0.0.1 -U project_bible -d pbdb -f ../../../Database/postgresql/postgres_table.sql
          psql -h 127.0.0.1 -U project_bible -d pbdb -f ../../../Database/postgresql/sample_data.sql

      - name: Generate Prisma Client
        run: npm run prisma:generate

      - name: Run Perf Smoke
        run: npm run test:perf:smoke

      - name: Upload Perf Smoke Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: javascript-express-npm-prisma-postgresql-perf-smoke
          path: BackEnd/JavaScript/javascript-express-npm-prisma-postgresql/test-results/perf-smoke-summary.json
          if-no-files-found: warn

  migration-validation-manual:
    if: github.event_name == 'workflow_dispatch' && inputs.run_migration_validation
    needs: validate-dispatch-inputs
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: pbdb
          POSTGRES_USER: project_bible
          POSTGRES_PASSWORD: project_bible
        options: >-
          --health-cmd="pg_isready -U project_bible -d pbdb"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=20
        ports:
          - 5432:5432
    defaults:
      run:
        working-directory: ${{ env.WORKDIR }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.target_ref }}

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: BackEnd/JavaScript/javascript-express-npm-prisma-postgresql/package-lock.json

      - name: Install Dependencies
        run: npm ci

      - name: Ensure PostgreSQL Client
        run: |
          if ! command -v psql >/dev/null 2>&1; then
            sudo apt-get update
            sudo apt-get install -y postgresql-client
          fi

      - name: Prepare Environment File
        run: |
          cat > .env <<EOF
          NODE_ENV=test
          PORT=8000
          APP_NAME=javascript-express-prisma
          API_PREFIX=/api/v1
          DATABASE_URL=${DATABASE_URL}
          DB_NAME=pbdb
          DB_USER=project_bible
          DB_PASSWORD=project_bible
          JWT_SECRET=ci-secret-key
          JWT_EXPIRES_IN=1h
          EOF

      - name: Bootstrap Shared PostgreSQL Schema
        env:
          PGPASSWORD: project_bible
        run: |
          psql -h 127.0.0.1 -U project_bible -d pbdb -f ../../../Database/postgresql/postgres_table.sql
          psql -h 127.0.0.1 -U project_bible -d pbdb -f ../../../Database/postgresql/sample_data.sql

      - name: Generate Prisma Client
        run: npm run prisma:generate

      - name: Run Migration Validation
        run: npm run test:script:validate-migrations

      - name: Upload Migration Validation Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: javascript-express-npm-prisma-postgresql-migration-validation
          path: BackEnd/JavaScript/javascript-express-npm-prisma-postgresql/test-results/migration-validation.json
          if-no-files-found: warn

  migration-roundtrip-manual:
    if: github.event_name == 'workflow_dispatch' && inputs.run_migration_roundtrip
    needs: validate-dispatch-inputs
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: pbdb
          POSTGRES_USER: project_bible
          POSTGRES_PASSWORD: project_bible
        options: >-
          --health-cmd="pg_isready -U project_bible -d pbdb"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=20
        ports:
          - 5432:5432
    defaults:
      run:
        working-directory: ${{ env.WORKDIR }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.target_ref }}

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: BackEnd/JavaScript/javascript-express-npm-prisma-postgresql/package-lock.json

      - name: Install Dependencies
        run: npm ci

      - name: Ensure PostgreSQL Client
        run: |
          if ! command -v psql >/dev/null 2>&1; then
            sudo apt-get update
            sudo apt-get install -y postgresql-client
          fi

      - name: Prepare Environment File
        run: |
          cat > .env <<EOF
          NODE_ENV=test
          PORT=8000
          APP_NAME=javascript-express-prisma
          API_PREFIX=/api/v1
          DATABASE_URL=${DATABASE_URL}
          DB_NAME=pbdb
          DB_USER=project_bible
          DB_PASSWORD=project_bible
          JWT_SECRET=ci-secret-key
          JWT_EXPIRES_IN=1h
          EOF

      - name: Bootstrap Shared PostgreSQL Schema
        env:
          PGPASSWORD: project_bible
        run: |
          psql -h 127.0.0.1 -U project_bible -d pbdb -f ../../../Database/postgresql/postgres_table.sql
          psql -h 127.0.0.1 -U project_bible -d pbdb -f ../../../Database/postgresql/sample_data.sql

      - name: Generate Prisma Client
        run: npm run prisma:generate

      - name: Run Migration Roundtrip
        run: npm run test:script:migration-roundtrip

      - name: Upload Migration Roundtrip Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: javascript-express-npm-prisma-postgresql-migration-roundtrip
          path: BackEnd/JavaScript/javascript-express-npm-prisma-postgresql/test-results/migration-roundtrip.json
          if-no-files-found: warn

  security-regression-manual:
    if: github.event_name == 'workflow_dispatch' && inputs.run_security_regression
    needs: validate-dispatch-inputs
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: pbdb
          POSTGRES_USER: project_bible
          POSTGRES_PASSWORD: project_bible
        options: >-
          --health-cmd="pg_isready -U project_bible -d pbdb"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=20
        ports:
          - 5432:5432
    defaults:
      run:
        working-directory: ${{ env.WORKDIR }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.target_ref }}

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: BackEnd/JavaScript/javascript-express-npm-prisma-postgresql/package-lock.json

      - name: Install Dependencies
        run: npm ci

      - name: Ensure PostgreSQL Client
        run: |
          if ! command -v psql >/dev/null 2>&1; then
            sudo apt-get update
            sudo apt-get install -y postgresql-client
          fi

      - name: Prepare Environment File
        run: |
          cat > .env <<EOF
          NODE_ENV=test
          PORT=8000
          APP_NAME=javascript-express-prisma
          API_PREFIX=/api/v1
          DATABASE_URL=${DATABASE_URL}
          DB_NAME=pbdb
          DB_USER=project_bible
          DB_PASSWORD=project_bible
          JWT_SECRET=ci-secret-key
          JWT_EXPIRES_IN=1h
          EOF

      - name: Bootstrap Shared PostgreSQL Schema
        env:
          PGPASSWORD: project_bible
        run: |
          psql -h 127.0.0.1 -U project_bible -d pbdb -f ../../../Database/postgresql/postgres_table.sql
          psql -h 127.0.0.1 -U project_bible -d pbdb -f ../../../Database/postgresql/sample_data.sql

      - name: Generate Prisma Client
        run: npm run prisma:generate

      - name: Run Security Regression
        run: |
          mkdir -p test-results
          npm run test:e2e:security-regression | tee test-results/security-regression.log

      - name: Upload Security Regression Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: javascript-express-npm-prisma-postgresql-security-regression
          path: BackEnd/JavaScript/javascript-express-npm-prisma-postgresql/test-results/security-regression.log
          if-no-files-found: warn

  admin-boundary-manual:
    if: github.event_name == 'workflow_dispatch' && inputs.run_admin_boundary
    needs: validate-dispatch-inputs
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: pbdb
          POSTGRES_USER: project_bible
          POSTGRES_PASSWORD: project_bible
        options: >-
          --health-cmd="pg_isready -U project_bible -d pbdb"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=20
        ports:
          - 5432:5432
    defaults:
      run:
        working-directory: ${{ env.WORKDIR }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.target_ref }}

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: BackEnd/JavaScript/javascript-express-npm-prisma-postgresql/package-lock.json

      - name: Install Dependencies
        run: npm ci

      - name: Ensure PostgreSQL Client
        run: |
          if ! command -v psql >/dev/null 2>&1; then
            sudo apt-get update
            sudo apt-get install -y postgresql-client
          fi

      - name: Prepare Environment File
        run: |
          cat > .env <<EOF
          NODE_ENV=test
          PORT=8000
          APP_NAME=javascript-express-prisma
          API_PREFIX=/api/v1
          DATABASE_URL=${DATABASE_URL}
          DB_NAME=pbdb
          DB_USER=project_bible
          DB_PASSWORD=project_bible
          JWT_SECRET=ci-secret-key
          JWT_EXPIRES_IN=1h
          EOF

      - name: Bootstrap Shared PostgreSQL Schema
        env:
          PGPASSWORD: project_bible
        run: |
          psql -h 127.0.0.1 -U project_bible -d pbdb -f ../../../Database/postgresql/postgres_table.sql
          psql -h 127.0.0.1 -U project_bible -d pbdb -f ../../../Database/postgresql/sample_data.sql

      - name: Generate Prisma Client
        run: npm run prisma:generate

      - name: Run Admin Boundary E2E
        run: |
          mkdir -p test-results
          npm run test:e2e:admin-boundary | tee test-results/admin-boundary.log

      - name: Upload Admin Boundary Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: javascript-express-npm-prisma-postgresql-admin-boundary
          path: BackEnd/JavaScript/javascript-express-npm-prisma-postgresql/test-results/admin-boundary.log
          if-no-files-found: warn

  release-gate:
    if: github.event_name == 'workflow_dispatch' && inputs.run_release_gate
    needs: validate-dispatch-inputs
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: pbdb
          POSTGRES_USER: project_bible
          POSTGRES_PASSWORD: project_bible
        options: >-
          --health-cmd="pg_isready -U project_bible -d pbdb"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=20
        ports:
          - 5432:5432
    defaults:
      run:
        working-directory: ${{ env.WORKDIR }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.target_ref }}

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: BackEnd/JavaScript/javascript-express-npm-prisma-postgresql/package-lock.json

      - name: Install Dependencies
        run: npm ci

      - name: Ensure PostgreSQL Client
        run: |
          if ! command -v psql >/dev/null 2>&1; then
            sudo apt-get update
            sudo apt-get install -y postgresql-client
          fi

      - name: Prepare Environment File
        run: |
          cat > .env <<EOF
          NODE_ENV=test
          PORT=8000
          APP_NAME=javascript-express-prisma
          API_PREFIX=/api/v1
          DATABASE_URL=${DATABASE_URL}
          DB_NAME=pbdb
          DB_USER=project_bible
          DB_PASSWORD=project_bible
          JWT_SECRET=ci-secret-key
          JWT_EXPIRES_IN=1h
          EOF

      - name: Bootstrap Shared PostgreSQL Schema
        env:
          PGPASSWORD: project_bible
        run: |
          psql -h 127.0.0.1 -U project_bible -d pbdb -f ../../../Database/postgresql/postgres_table.sql
          psql -h 127.0.0.1 -U project_bible -d pbdb -f ../../../Database/postgresql/sample_data.sql

      - name: Generate Prisma Client
        run: npm run prisma:generate

      - name: Run Release Gate
        run: npm run test:release:gate

      - name: Upload Release Gate Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: javascript-express-npm-prisma-postgresql-release-gate
          path: |
            BackEnd/JavaScript/javascript-express-npm-prisma-postgresql/test-results/release-gate-summary.json
            BackEnd/JavaScript/javascript-express-npm-prisma-postgresql/test-results/migration-validation.json
            BackEnd/JavaScript/javascript-express-npm-prisma-postgresql/test-results/contract-doc-summary.json
            BackEnd/JavaScript/javascript-express-npm-prisma-postgresql/test-results/openapi.json
            BackEnd/JavaScript/javascript-express-npm-prisma-postgresql/test-results/routes.snapshot.json
          if-no-files-found: warn

      - name: Write Release Gate Summary
        if: always()
        run: |
          echo "## JavaScript Release Gate Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- Trigger: \`workflow_dispatch\`" >> $GITHUB_STEP_SUMMARY
          echo "- Target ref: \`${{ inputs.target_ref }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- Job result: \`${{ job.status }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- Artifact: \`javascript-express-prisma-release-gate\`" >> $GITHUB_STEP_SUMMARY
          echo "- Run URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}" >> $GITHUB_STEP_SUMMARY
